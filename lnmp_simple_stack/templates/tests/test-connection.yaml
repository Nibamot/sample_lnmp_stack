apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lnmp_simple_stack.fullname" . }}-test-connection"
  labels:
    {{- include "lnmp_simple_stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lnmp_simple_stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
