apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysql-app.fullname" . }}-test-connection"
  labels:
    {{- include "mysql-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mysql-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
